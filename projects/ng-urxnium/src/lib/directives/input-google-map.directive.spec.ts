import { InputGoogleMapDirective } from './input-google-map.directive';

describe('GoogleMapDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new InputGoogleMapDirective();
    expect(directive).toBeTruthy();
  });
});
