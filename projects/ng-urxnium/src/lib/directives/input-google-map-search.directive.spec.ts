import { GoogleMapSearchDirective } from './google-map-search.directive';

describe('GoogleMapsSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleMapSearchDirective();
    expect(directive).toBeTruthy();
  });
});
